// .forEach

// 1.   Given an array of numbers, log them to the console for each number. 
const nums = [1,2,3,4,5]
nums.forEach(element => console.log(element))

// 2.   Given an array of people, log a greeting to the console for each person.
const people = ["joe","Joe","Joe"]
people.forEach(element => console.log("Hello"+ " " ,element))

// 3.   Given an array of mixed values, log whether it contains an undefined value.
const random = [1,2,3,'hi',"hELLO"]
random.forEach(element => console.log(element))

// 4.   Given an array of strings, log the vowel count for each string.
const vowels = ["a", 'e', 'i', 'o', 'u'];

const str = ["Hello World Pizza is good"];

vowels.forEach(word => console.log(element))

// 5.   Given an array of numbers, duplicate them and log them to the console. 

const nums2 = [1,2,3,4,5]
nums2.forEach(element => console.log(element*2))